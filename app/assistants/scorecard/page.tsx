import { ScorecardAssistant } from '@/components/assistants/ScorecardAssistant';

export const dynamic = 'force-dynamic';

export default function ScorecardPage() {
  return <ScorecardAssistant />;
}
